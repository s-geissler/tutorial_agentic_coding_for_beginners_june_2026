// Picture slide has no slide-specific behaviour. The lightbox widget
// (in templates/default/widgets.js) handles the click-to-zoom automatically
// because the <img> has the data-lightbox attribute.
